# 「깊은 곳」 — 선언

이 문서는 **결정을 미리 내려 두는 곳**이다. 여기 적힌 것과 다투는 코드는
틀린 코드이고, 여기 없는 것은 아직 결정되지 않은 것이다.

왜 필요한가: 이 저장소는 지금까지 아래에서 위로 만들어졌다. 신 다섯을
설계한 **뒤에** 「신앙이 깊어질수록 기괴해진다」가 왔고, 그래서 그 다섯을
다시 잡아야 했다. 유물 마흔을 「기괴하게」 다시 그리려면 8×8에서 기괴함이
무엇인지 먼저 있어야 하는데, 그것도 없었다. 선언이 먼저다.

---

## 0. 이 게임이 무엇인가

한 손으로, 세로로, 모바일 브라우저에서 하는 로그라이크. 15층. 한 판
2,000–4,000턴. 죽으면 끝난다.

만드는 사람과 노는 사람이 같고, 친구 몇이 더 있다. **대중을 겨냥하지
않는다** — 그래서 어려워도 되고, 불친절해도 되고, 기괴해도 된다. 다만
**고장 나 보이면 안 된다.** 이 게임에서 「눌렀는데 아무 일도 안 일어났다」는
거의 언제나 버그로 읽히고, 그것이 이 저장소가 가장 자주 고쳐 온 종류의
결함이다.

---

## 1. 세계관

신이 예언했다. 지하 깊은 곳에 악의 근원이 있고, 그것을 무찌르면 세상에
평화가 온다고. 용사는 계시에 따라 선택받아 내려간다.

내려갈수록 **신앙심이 짙어진다.** 저절로 그렇게 된다. 그리고 짙어질수록
왜곡된다 — 벽이 다르게 보이고, 신의 말이 더 자주 들리고, 손이 신이 원하는
쪽으로 움직인다.

가장 깊은 곳에서 악의 근원을 물리친다. 그것은 **전대 용사**였다.

그리고 나는 그 자리에서 **새로운 보스가 된다.** 신은 처음부터 거대한
악마였고, 용사들은 전부 속아넘어간 것이다. 세상은 계속 멸망으로 치닫는다.
다음 용사가 나를 죽이러 올 것이다.

### 그래서 이 게임은

- **신앙심은 자원이 아니라 저주다.** 쌓이는 것이지 모으는 것이 아니다
- **신의 선물은 미끼다.** 강해지는 대가로 왜곡된다
- **정상 클리어는 실패다.** 이기면 다음 판의 보스가 된다
- **진 엔딩은 신앙 없이 끝내는 것이다.** 아무 버프도 안 받고, 유혹에 한
  번도 안 넘어가고, 심연 8단에서. 그리고 **거절할 자격은 사다리를 여덟
  번 올라야 생긴다**

이 마지막 줄이 이 게임의 최고 난이도이고, **그 난이도를 시스템이 강요하지
않는다 — 플레이어가 고른다.** 4·8·12층에서 세 번 거절하면 그 판은
버프가 하나도 없는 판이 되고, 그 판만이 진짜 끝을 볼 수 있다.

### 신은 하나다 — 얼굴이 다섯일 뿐

다섯 신을 고르는 것처럼 보이지만 전부 같은 것이다. 어느 얼굴을 골라도
같은 곳으로 데려간다. 마지막에 그것이 드러난다.

이 사실은 **첫 판에는 어디에도 안 적혀 있다.** 두 번째 판에 다섯 중 다른
얼굴을 고르고서야 같은 말투를 알아본다.

### 다음 용사는 나를 죽이러 온다

최심부의 보스는 **당신의 지난 판 영웅**이다. `meta.last` 가 이미 그것을
갖고 있다 — 종족·직업·레벨·무기·유물까지. 처음 내려가는 사람에게는
이름 없는 앞선 자가 서 있다.

이 게임은 혼자 또는 친구 몇이서 반복해서 노는 게임이다. 그러니 판이
판을 먹는 구조가 값을 한다.

### DCSS에서 무엇을 빌리고 무엇을 안 빌리는가

[Dungeon Crawl Stone Soup](https://github.com/crawl/crawl)를 **설계 참고로만**
쓴다. 코드도, 텍스트도, 아트도 한 줄 안 가져온다.

| | 빌린다 | 안 빌린다 |
|---|---|---|
| 코드 | — | 51만 줄 C++. 우리는 2.4만 줄 바닐라 JS다 |
| 타일 | — | 8,622개 32×32 PNG. 우리는 이미지 파일이 0개다 |
| 텍스트 | — | GPL이고, 목소리가 다르다 |
| **설계** | **신·계율·종족 규칙의 구조** | 숫자 |

숫자를 안 빌리는 이유: 저쪽 저울은 15시간·27분기·수천 턴 신앙심을
전제한다. 여기는 15층·3,000턴이다. **구조만 빌리고 값은 우리 벤치로
잰다.** 이 세션에서 남의 자로 우리 게임을 고치려다 두 번 실패했다.

그리고 DCSS의 신은 **고르는 것**이지만 여기의 신은 **속이는 것**이다.
구조는 같아도 방향이 반대다.

라이선스는 **MIT 그대로**다. 설계는 저작권 대상이 아니므로 GPL 전염이
없다. 이 선은 넘지 않는다.

---

## 2. 톤

### 담담하게 쓴다

이 게임의 목소리는 **간결한 문어체**다. 2인칭, 짧은 문장, 감탄 없음.

> 불을 껐다 — 처음으로.
> 상처가 남았다. 견딜 수 있는 몸이 4만큼 줄었다.
> 이 층에서는 이미 한 번 썼다.

### 기괴함과 담담함은 안 싸운다

새 방향은 기괴하다 — 눈알로 된 벽, 팔다리로 된 복도, 비명 지르는
얼굴가죽, 광신, 글리치. 그런데 이 둘은 충돌하지 않는다. **코즈믹 호러의
정석이 정확히 담담한 서술이다.** 호들갑을 떨면 만화가 되고, 한 줄로
적으면 세 배 기괴해진다.

> 벽이 젖어 있다. 만지면 따뜻하다.        ← 이렇게
> 끔찍하게도 벽이 살아 숨쉬고 있었다!!    ← 이러지 않는다

**규칙:** 기괴한 것일수록 더 짧게 적는다. 설명하지 않는다. 놀라지 않는다.

### 세 가지 목소리만 있다

| 누가 | 어떻게 |
|---|---|
| 서술 | 담담한 문어체. 위와 같다 |
| 신 | 명령형. 짧고, 이유를 안 댄다. 「마시지 마라.」 |
| 이물 | 아무 말도 안 한다. 규칙만 바뀐다 |

---

## 3. 그래픽 문법

### 지금 있는 것

- 스프라이트는 **8×8 문자 격자**다(`pixels.js`). 이미지 파일은 0개
- 팔레트 **26색** 고정. `.`은 투명
  ```
  k 검정  d 어둠  g 회색  G 밝은회색  w 뼈  W 흰빛
  r 핏빛  R 붉음  o 주황  y 금빛  n 갈색  N 밝은갈색
  e 짙은초록  E 초록  b 짙은파랑  B 파랑  p 자주  P 밝은자주
  c 청록  s 강철  A/D/F/H/J 지형용 어둠들
  ```
- 렌더러에 이미지 경로가 없다. 앞으로도 안 만든다

### 왜 8×8을 지키는가

390px 폰에서 한 화면에 들어가는 칸 수가 이 게임의 전투 반경 전체다.
32×32로 가면 보이는 칸이 1/4로 줄고, 그러면 「어디에 설 것인가」라는
이 게임의 유일한 동사가 죽는다.

### 기괴함을 8×8로 어떻게 그리는가

여덟 칸 안에서 기괴함은 **디테일이 아니라 규칙 위반**으로 표현된다.
털을 그릴 자리가 없으므로, 대신 **있어야 할 것이 없거나 하나 더 있게**
한다.

1. **대칭을 깬다.** 이 게임의 스프라이트는 대부분 좌우 대칭이다. 한쪽만
   틀어진 실루엣이 8×8에서 가장 빠르게 「잘못됐다」로 읽힌다
2. **수를 틀린다.** 눈 셋, 팔 다섯, 다리 하나. 세는 순간 알아차린다
3. **눈은 크게, 나머지는 뭉갠다.** 8×8에서 읽히는 기관은 눈뿐이다
4. **살색을 안 쓴다.** `N`(밝은갈색)과 `w`(뼈)만으로 살을 그린다 —
   붉은 살은 고기로 읽히고, 그건 기괴한 게 아니라 더럽다
5. **움직이는 쪽으로 기괴함을 옮긴다.** 정지 화면에서 못 하는 것을
   프레임이 한다: 시선이 따라오고, 한 프레임 늦게 따라오고, 안 따라온다

### 글리치 문법

글리치는 **팔레트 안에서** 한다. 새 색을 만들지 않는다.

| 표현 | 방법 |
|---|---|
| 색 분리 | 같은 스프라이트를 `R`/`B`로 1px 어긋나게 두 번 |
| 찢김 | 가로 한 줄을 n픽셀 밀어서 그린다 |
| 잔상 | 이전 프레임을 `d`로 한 겹 남긴다 |
| 오독 | 타일 하나를 **다른 타일로** 그린다(규칙은 안 바뀐다) |

마지막이 가장 값싸고 가장 무섭다 — 이미 `static` 이물 층이 그 규칙을
갖고 있다.

### 지키는 린트

이 넷은 기준선을 박아 두고 「나빠지지 않는가」만 본다. 기괴함이 가독성을
이기면 안 된다.

| 벤치 | 지키는 것 |
|---|---|
| `silhouette.mjs` | 스프라이트끼리 실루엣이 안 겹친다 |
| `contrast.mjs` | 바닥 위에서 3:1 이상 |
| `knots.mjs` | 8×8 안에서 형태가 안 뭉갠다 |
| `icons.mjs` | 아이콘 마흔일곱이 서로 구별된다 |
| `stage.mjs` | 지형이 화면에서 읽힌다 |

기준선을 **올리려면 왜인지 적어야 한다.** 내리는 것(개선)은 그냥 한다.

---

## 4. 시스템 축

축을 늘리지 않는다. 늘리면 서로를 안 보고 각자 논다.

### 자원 — 통 하나, 이름 여섯

기력 하나. 직업마다 이름만 다르다(전사 기력 · 궁수 호흡 · 도적 그림자 ·
사제 신앙 · 팔라딘 맹세). 정체성은 통이 아니라 **차는 규칙**에 있다.
마나는 유틸 전용. 체력·마나·기력의 회복 속도는 각기 다른 능력치가 정한다.

### 신은 수치를 안 속인다 — 속이는 것은 결과다

선물의 설명도 계율도 **서약 전에 전부 말한다.** 성능을 속이면 플레이어가
값을 못 재고, 값을 못 재는 선택지는 선택지가 아니다 — §0의 「고장 나
보이면 안 된다」와 정면으로 부딪힌다.

신이 속이는 것은 **「무찌르면 평화가 온다」** 쪽이다. 강해져서 내려가는
것 자체가 **다음 용사가 만날 더 강한 악마를 빚는 과정**이고, 받은 선물
하나하나가 다음 판의 보스에게 그대로 얹힌다. 그 사실은 이 판에서
어디에도 안 적혀 있다.

그래서 거짓말은 값에 없고 **결과에** 있다. 그리고 그 결과는 이 판이
아니라 다음 판에서 드러난다.

### 신 — 아르카나를 대체한다

4·8·12층. 지금 아르카나가 쓰는 리듬을 그대로 쓴다. 다만 고르는 것이
**셋이 아니라 넷**이다:

```
받는다 ①   받는다 ②   받는다 ③   ── 그리고 네 번째 ──   거절한다
```

거절은 아무것도 안 준다. 신앙심도 안 오른다.

### 거절은 심연 8단에서만 열린다

**첫 판부터 네 번째 칸은 화면에 있다. 다만 잠겨 있다.**

```
거절한다     ✕   아직 거절할 수 있는 자가 아니다
```

숨기지 않는 이유: 위키도 공략도 없는 게임이다. 완전히 감추면 아무도
못 찾고, 못 찾는 진 엔딩은 없는 진 엔딩이다. 잠긴 채로 보이면 **왜
잠겼는지 알고 싶어진다** — 그것이 사다리를 오르는 이유가 된다.

심연은 **오르는 것이지 고르는 것이 아니다**(`setAbyss` 가 `cleared()+1`
로 자른다). 그래서 8단에 서려면 0단부터 7단까지 차례로 이겨야 하고,
족쇄는 누적이라 8단에서는 여덟 개를 전부 진다:

  굶주린 불 · 마른 샘 · 깨어 있는 것들 · 식은 모루 ·
  무거운 것들 · 닫힌 장부 · 긴 그림자 · 재의 무게

여기에 **신의 선물 없음**이 얹힌다. 진 엔딩은 그렇게 두 번 벌어진다 —
사다리를 여덟 번 오르고, 마지막 판에서 세 번 거절하고.

### 반드시 재야 하는 것 하나

**8단 + 선물 없음이 실제로 이길 수 있는 판인가.** 못 이기는 진 엔딩은
난이도가 아니라 버그다. 이건 봇으로 못 잰다 — 봇은 0단에서도 8~10층에서
죽는다. §6대로 **사람의 판 기록으로** 확인한다.

만약 못 이기는 것으로 드러나면, 고치는 방향은 **족쇄를 약하게 하는 쪽이
아니다.** 거절에 무언가를 붙이면 「거절」이 아니게 된다. 그때는 8단의
족쇄 구성을 다시 짜거나, 진 엔딩의 조건을 「셋 다 거절」에서 「하나도
받지 않고 12층까지」로 옮긴다.

핵심은 권능이 아니라 **계율**이다. 신은 버튼을 주는 게 아니라
**금지한다.** 「피를 세는 자」에게 서약하면 그 판은 물약이 봉인된 판이
되고, 남은 열한 층을 다르게 걷는다.

### 신앙심 — 자원이 아니라 저주다

두 갈래로 오른다:

| 어디서 | 얼마나 |
|---|---|
| **층을 내려가는 것** | 저절로. 막을 수 없다 |
| **선물을 받는 것** | 크게. 이쪽만 막을 수 있다 |

그래서 아무것도 안 받아도 신앙심은 천천히 오르고, 셋을 다 받으면 12층
전에 광신에 닿는다. 「짙어진다」는 서술이 규칙이 되는 자리다.

그리고 신앙심은 숫자가 아니라 **화면**이다:

| 신앙심 | 화면 |
|---|---|
| 0–35 | 정상 |
| 35–70 | 벽과 UI가 미묘하게 어긋난다. 잔상이 한 프레임 늦다 |
| 70–100 | 광신 — 글리치, 색 분리, **계율을 어기는 버튼이 손가락을 피한다** |

마지막 줄이 이 시스템의 값이다. 「신이 강요한다」를 문장으로 말하는 대신
버튼이 실제로 말을 안 듣게 만든다.

### 엔딩 — 이기는 것이 실패다

최심부에는 **전대 용사**가 서 있다. `meta.last` 로 짓는다 — 당신의 지난
판 영웅이 종족·직업·레벨·무기·유물 그대로 온다.

그를 이긴 뒤에 무슨 일이 일어나는지가 **신앙심으로 갈린다:**

| 신앙심 | 끝 |
|---|---|
| 높음 (광신) | 그 자리에 앉는다. 다음 판의 보스가 된다. 세상은 계속 무너진다 |
| 낮음 | 앉지 않는다. 신이 무엇인지 보이고, **그것이 진 엔딩이다** |

진 엔딩은 **심연 8단에서 버프를 하나도 안 받은 판**에서만 나온다. 가장
약한 몸으로 가장 나쁜 조건에서 가장 깊은 곳까지 가는 것 — 이 게임의
최고 난이도이고, 그 난이도를 **플레이어가 스스로 고른다.** 그리고 그
자격은 사다리를 여덟 번 올라서 얻는다.

이긴 판의 영웅은 `meta` 에 남아 다음 판의 보스가 된다. 진 엔딩으로
끝낸 판은 남지 않는다 — 앉지 않았으니까.

### 종족 — 보정이 아니라 규칙

여덟 종족 각각 **규칙 하나와 대가 하나**. 대가가 없으면 그냥 더 좋은
종족이 하나 생길 뿐이다. 전부 이미 있는 깔때기를 지난다.

### 직업 — 8칸

기예 4(직업 자원) + 유틸 4(마나).

| | 기예 | 유틸 |
|---|---|---|
| 1 | 기본 공격 | 경상 치유 (공통) |
| 2 | 직업특화 공격 | 강화 치유 (공통) |
| 3 | 단점 상쇄 | 감지 (직업별) |
| 4 | 궁극기 | 회피·이동 (직업별) |

마법사만 예외 — 기예 없이 주문 8개.

무기 계열이 직업을 제한한다. 전사는 활과 지팡이를 못 든다. 「쓸 수는
있는데 나쁘다」는 선택이 아니라 함정이다.

### 손맛 — 연출은 등급이다

스킬마다 연출을 따로 짜면 스물넷이 각자 다른 말을 한다.

| 등급 | 정지 | 늘어짐 | 렌즈 |
|---|---|---|---|
| 한 대 (치명타·기본) | 70ms | — | — |
| 특화 | 110ms | 0.45배 260ms | — |
| 궁극 | 180ms | 0.30배 320ms | 1.25배 줌인 후 복귀 |

늘어짐 뒤에 **빨리감기**가 붙는 이유가 「이펙트 몰아서」다 — 늘어지는
동안 파편이 제자리에 머물고, 당겨질 때 한꺼번에 흩어진다.

치명타에 늘어짐이 없는 것은 의도다. 자주 터지므로, 매번 늘어지면 세
번째 판부터는 연출이 아니라 지연이다.

### 이물 — 설명하지 않는 층

열 판에 한 층쯤. 후반에만. 규칙이 통째로 다르고, 아무 말도 안 한다.

---

## 5. 구조 규칙

이건 취향이 아니라 이 저장소가 지켜 온 계약이다.

0. **세계관을 아는 곳은 문장뿐이다.** 규칙은 세계관을 모른다 — 신앙심은
   `game.js` 에서 그냥 0~100짜리 숫자이고, 그것이 저주라는 것은 이 문서와
   화면이 안다. 그래야 헤드리스 봇이 계속 돈다
1. **게임 룰은 `game.js`에만.** DOM도 localStorage도 안 쓴다 — 헤드리스
   시뮬이 돌아야 하기 때문이다
2. **단일 깔때기.** 같은 질문에 답하는 자리는 하나여야 한다.
   `hurtPlayer` · `recalc` · `gearBonus` · `poolGain` · `cantHold` ·
   `artCost` · `learned` · `mendWound` · `fx` · `say`
   문이 갈리면 「눌리는데 안 나가는 버튼」이 생기고, 봇은 거기서 영영
   돈다. 이 저장소는 그 사고를 세 번 겪었다
3. **커밋 메시지는 한국어로.** 무엇을 왜 바꿨는지와 **측정 숫자**를 담아서

---

## 6. 측정 원칙

이 저장소에서 가장 자주 틀린 것은 게임이 아니라 **자**였다.

1. **숫자가 이상하면 게임을 의심하기 전에 탐침과 봇을 먼저 의심한다.**
   한 세션에 다섯 번 그 실수를 했다
2. **3배치 복제.** 한 배치로 읽으면 ±1.5층이 그냥 흔들린다. 같은 설정에서
   하프트롤이 5.98과 8.36으로 나온 적이 있다
3. **문턱은 노이즈에서 뽑는다.** 배치 폭보다 작은 차이를 잡으면 벤치가
   제 그림자를 쫓는다
4. **최상 궁합으로 판정한다.** 종족은 궁합이 맞는 직업으로 잰다 — 엘프를
   전사로 재는 것은 엘프에 대해 아무것도 안 말한다
5. **최악의 선택은 완화하지 않는다.** 하프트롤 마법사가 5층에서 죽는 것은
   고장이 아니라 그 선택의 값이다. 완화를 넣으면 고르는 일이 의미를 잃는다
6. **간헐적으로 우는 벤치는 안 우는 것만 못하다**

### 살아 있는 사람의 판이 봇보다 낫다

봇은 8층에서 죽고 사람은 15층을 클리어한다. 그 간극을 손으로 메우려다
성장 곡선을 통째로 틀리게 잡은 적이 있다. 조작법 화면에서 판 기록을
파일로 받을 수 있고, `sim/replay.mjs`가 그것을 읽는다. **밸런스는 봇으로
재현하지 말고 사람의 판으로 본다.**

---

## 7. 순서

**진행 상황과 다음에 집을 것은 `NEXT.md` 에 있다.** 세션은 안 이어지므로
그 파일이 유일한 인수인계다.

```
0. 선언           ← 이 문서 (세계관까지 확정)
1. 그래픽 문법     8×8에서 기괴함이 무엇인가 (§3을 코드로)
2. 신 시스템       아르카나 대체 · 거절 · 계율 · 신앙심 기괴화 · 엔딩 둘
3. 종족·직업·스킬   신 위에서 설계
4. 아이템·유물     그래픽 문법 위에서
5. 이물 층 확장
```

2번이 이 게임에서 가장 큰 덩어리다. 안에서 다시 넷으로 쪼갠다:
서약과 거절 → 신앙심과 계율 → 기괴화 → 전대 용사와 엔딩 둘.

신이 직업보다 먼저인 이유: 계율이 직업 유틸 칸의 값을 바꾼다. 「피를 세는
자」가 물약을 봉인하면 경상 치유 칸이 그 판에서 무의미해진다. 그걸 모르고
여덟 칸을 설계하면 두 번 일한다.

그래픽 문법이 신보다 먼저인 이유: 신앙심 기괴화·글리치·이물 층·유물이
전부 같은 시각 언어를 써야 한다. 그 협상은 한 번만 한다.
